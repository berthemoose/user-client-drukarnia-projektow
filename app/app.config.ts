export default defineAppConfig({
    ui: {
        primary: "slate",
        gray: "cool",
    },
    app: {
        head: {
            htmlAttrs: {
                lang: 'pl'
            }
        }
    }
})